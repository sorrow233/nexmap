/**
 * Modular translations index
 * Aggregates all language files into a single translations object
 */
import { en } from './en';
import { zh } from './zh';
import { ja } from './ja';
import { ko } from './ko';

const translations = {
    en,
    zh,
    ja,
    ko
};

export default translations;
